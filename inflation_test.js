//this is poopy right now, come back to it later, you are juggling too many things at once
function desiredTime(years, priciple) {
   // var equation = priciple * ();
}

function accumulation(time, nominalAnnualInterestRate){
    return 1 + (time * nominalAnnualInterestRate)
}


var masterEquation = accumulation() * (1 + ((nominalAnnualInterestRate / (time * periods)  ) ^ floor(time * periods)) )

function masterCalculator(time, nominalAnnualInterestRate, ) {

}